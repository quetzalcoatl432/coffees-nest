import { StatusValidationPipe } from './status-validation.pipe';

describe('StatusValidationPipe', () => {
  it('should be defined', () => {
    expect(new StatusValidationPipe()).toBeDefined();
  });
});
